% 文件: MONTE.M
% 说明: 自动添加的注释占位，请根据需要补充。
% 生成: 2025-08-31 23:06
% 注释: 本文件头由脚本自动添加

%MonteCarlo法解最优化的例
clear;  % 详解: 执行语句
vmax=-inf;  % 详解: 赋值：计算表达式并保存到 vmax
x2=specrnd(10:20,[],1,5);  % 详解: 赋值：将 specrnd(...) 的结果保存到 x2
x3=specrnd(-5:16,[],1,5);  % 详解: 赋值：将 specrnd(...) 的结果保存到 x3
for i=1:5  % 详解: for 循环：迭代变量 i 遍历 1:5
   for j=1:5  % 详解: for 循环：迭代变量 j 遍历 1:5
      if x2(i)+2*x3(j)>=10&3*x2(i)+2*x3(j)<=62,  % 详解: 条件判断：if (x2(i)+2*x3(j)>=10&3*x2(i)+2*x3(j)<=62,)
         v=(x2(i)+10)*x2(i)*x3(j);  % 详解: 赋值：计算表达式并保存到 v
         if v>vmax,  % 详解: 条件判断：if (v>vmax,)
            vmax=v;x20=x2(i);x30=x3(j);  % 详解: 赋值：计算表达式并保存到 vmax
         end  % 详解: 执行语句
      end  % 详解: 执行语句
   end  % 详解: 执行语句
end  % 详解: 执行语句
x1=x20+10,x2=x20,x3=x30  % 详解: 赋值：计算表达式并保存到 x1
vmax  % 详解: 执行语句




